<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.pivotal.cf.mobile.pushsdk"
    android:versionCode="4"
    android:versionName="0.4">

	<!--
    <uses-sdk android:minSdkVersion="10" android:targetSdkVersion="19" />
	-->

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <uses-permission android:name="android.permission.GET_ACCOUNTS"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <uses-permission android:name="com.google.android.c2dm.permission.RECEIVE"/>

    <application android:allowBackup="true"
        android:label="@string/app_name"
        android:icon="@drawable/ic_launcher"
        android:theme="@style/AppTheme">

        <meta-data android:name="com.google.android.gms.version" android:value="@integer/google_play_services_version" />

        <receiver android:name="com.pivotal.cf.mobile.pushsdk.broadcastreceiver.BootCompletedBroadcastReceiver" >
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <receiver android:name="com.pivotal.cf.mobile.pushsdk.broadcastreceiver.EventsSenderAlarmReceiver" />

        <service android:name="com.pivotal.cf.mobile.pushsdk.service.GcmService"/>

        <service android:name="com.pivotal.cf.mobile.pushsdk.service.EventService"/>

    </application>

</manifest>
